# Cloudflare Workers Assets _redirects supports RELATIVE paths only — no hostname
# redirects. www -> apex canonicalization must be a Redirect Rule in the Cloudflare
# dashboard (Rules > Redirect Rules). Every page's <link rel="canonical"> already
# points to the apex domain, so search engines consolidate regardless.

# Collapse the explicit index.html URL to the clean root path.
/index.html / 301
